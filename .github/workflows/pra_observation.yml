name: Update PRA Nighttime Results (MATLAB)

on:
  schedule:
    - cron: '0 18 * * *'  
  workflow_dispatch:

jobs:
  update-pra:
    name: Run PRA Nighttime Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        lfs: true

    - name: Set up MATLAB
      uses: matlab-actions/setup-matlab@v1

    - name: Run PRA Detection Script
      uses: matlab-actions/run-command@v1
      with:
        command: PRA_Nighttime_KAK

    - name: Commit and Push Results
      run: |
        git config --global user.name 'syaifulafrizal'
        git config --global user.email 'syaifulafrizal@gmail.com'
        if ls INTERMAGNET_DOWNLOADS/**/*.mat INTERMAGNET_DOWNLOADS/**/*.png INTERMAGNET_DOWNLOADS/**/*.txt README.md 1> /dev/null 2>&1; then
          git add INTERMAGNET_DOWNLOADS/**/*.mat INTERMAGNET_DOWNLOADS/**/*.png INTERMAGNET_DOWNLOADS/**/*.txt README.md
          git commit -m "📡 Daily PRA and Threshold update" || echo "No changes to commit"
          git push
        else
          echo "⚠️ No new output files to add."
        fi

    - name: Display PRA Plot in Summary
      run: |
        echo "### 🛰️ Latest PRA Nighttime Plot" >> $GITHUB_STEP_SUMMARY
        echo "![PRA Plot](./INTERMAGNET_DOWNLOADS/figures/PRA_$(date +'%Y%m%d').png)" >> $GITHUB_STEP_SUMMARY
